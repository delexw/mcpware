{
  "backends": [
    {
      "name": "example",
      "command": [
        "echo",
        "Container mode: External backends should be configured as separate services"
      ],
      "description": "Example backend - configure as separate container",
      "timeout": 30
    }
  ]
}